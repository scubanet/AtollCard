# Dark Mode + A11y-Härtung Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** System-Dark-Mode + Accessibility-Härtung (Dynamic Type, Kontrast, Reduce Transparency/Motion) über die Design-System-Tokens.

**Architecture:** Theme-Tokens werden `Color(light:dark:)`-dynamisch → alle Nutzungen adaptieren automatisch. `Font.atoll` skaliert via `relativeTo`. `GlassBackground` + Standalone-Materials degradieren bei „Transparenz reduzieren"; custom Animationen respektieren Reduce Motion.

**Tech Stack:** SwiftUI, UIKit/AppKit (dynamic Color bridge), XCTest, XcodeGen, Swift 5.

**Konventionen:** Repo `~/Developer/AtollCard`. Nach Änderungen `xcodegen generate`. iOS-Test `xcodebuild test -scheme AtollCard -destination 'platform=iOS Simulator,name=iPhone 16e,OS=26.2'`; macOS `-destination 'platform=macOS,arch=arm64'`. Commits enden mit `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## File Structure
- `AtollCard/DesignSystem/Theme.swift` — `Color(light:dark:)` + dynamische Tokens + Kontrast-Fix.
- `AtollCard/DesignSystem/FontRegistration.swift` — Dynamic-Type `relativeTo`.
- `AtollCard/DesignSystem/GlassBackground.swift` — Reduce-Transparency-Fallback.
- `AtollCard/Features/Shell/FloatingTabBar.swift`, `AtollCard/Features/Shell/MyCardScreen.swift` — Standalone-Material-Fallback; Reduce-Motion.
- `AtollCard/Features/CardEditor/OnboardingView.swift` — Reduce-Motion.
- Test: `AtollCardTests/ThemeColorTests.swift`.

---

## Task 1: Dynamische Theme-Tokens + Kontrast-Fix

**Files:** Modify `AtollCard/DesignSystem/Theme.swift`; Test `AtollCardTests/ThemeColorTests.swift`.

- [ ] **Step 1: Failing test** `AtollCardTests/ThemeColorTests.swift`
```swift
import XCTest
import SwiftUI
@testable import AtollCard

final class ThemeColorTests: XCTestCase {
    #if os(iOS)
    func test_appBGResolvesDifferentlyInDarkMode() {
        let light = UIColor(Theme.appBG).resolvedColor(with: UITraitCollection(userInterfaceStyle: .light))
        let dark  = UIColor(Theme.appBG).resolvedColor(with: UITraitCollection(userInterfaceStyle: .dark))
        XCTAssertNotEqual(light, dark, "appBG must differ between light and dark")
    }
    func test_textResolvesDifferentlyInDarkMode() {
        let light = UIColor(Theme.text).resolvedColor(with: UITraitCollection(userInterfaceStyle: .light))
        let dark  = UIColor(Theme.text).resolvedColor(with: UITraitCollection(userInterfaceStyle: .dark))
        XCTAssertNotEqual(light, dark)
    }
    #endif
}
```

- [ ] **Step 2: Run → FAIL** (`Theme.appBG` ist noch statisch → resolved light == dark).
Run: `xcodebuild test -scheme AtollCard -destination 'platform=iOS Simulator,name=iPhone 16e,OS=26.2' -only-testing:AtollCardTests/ThemeColorTests 2>&1 | grep -iE "Executed|failed|TEST"`

- [ ] **Step 3: Implement** — `AtollCard/DesignSystem/Theme.swift` ersetzen durch:
```swift
import SwiftUI

enum Theme {
    static let accentDefault = Color(light: Color(hex: "#0E7C86"), dark: Color(hex: "#16A8B4"))
    static let appBG    = Color(light: Color(hex: "#EEEFF2"), dark: Color(hex: "#0E1116"))
    static let surface  = Color(light: Color(hex: "#FFFFFF"), dark: Color(hex: "#1C1E24"))
    static let surface2 = Color(light: Color(hex: "#F2F2F6"), dark: Color(hex: "#2A2D34"))
    static let text     = Color(light: Color(hex: "#14161A"), dark: Color(hex: "#F2F3F5"))
    static let text2    = Color(light: Color(hex: "#5E636B"), dark: Color(hex: "#9AA0AA"))
    static let separator = Color(light: Color(red: 60/255, green: 60/255, blue: 67/255).opacity(0.10),
                                 dark: Color.white.opacity(0.14))
    static let font = "Manrope"   // bundled variable font; fallback to system
}

extension Color {
    init(hex: String) {
        let s = hex.hasPrefix("#") ? String(hex.dropFirst()) : hex
        var v: UInt64 = 0; Scanner(string: s).scanHexInt64(&v)
        self.init(.sRGB,
                  red: Double((v >> 16) & 0xFF) / 255,
                  green: Double((v >> 8) & 0xFF) / 255,
                  blue: Double(v & 0xFF) / 255, opacity: 1)
    }

    /// Resolves to `light` or `dark` based on the active interface style.
    init(light: Color, dark: Color) {
        #if os(iOS)
        self = Color(UIColor { $0.userInterfaceStyle == .dark ? UIColor(dark) : UIColor(light) })
        #elseif os(macOS)
        self = Color(NSColor(name: nil) { appearance in
            appearance.bestMatch(from: [.darkAqua, .aqua]) == .darkAqua ? NSColor(dark) : NSColor(light)
        })
        #else
        self = light
        #endif
    }
}
```

- [ ] **Step 4: Run → PASS** (2 Tests, iOS). macOS-Build grün (`NSColor(name:dynamicProvider:)` ist verfügbar).

- [ ] **Step 5: Commit**
```bash
git add AtollCard/DesignSystem/Theme.swift AtollCardTests/ThemeColorTests.swift
git commit -m "feat(ios): dynamic light/dark Theme tokens + text2 contrast fix

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Dynamic Type

**Files:** Modify `AtollCard/DesignSystem/FontRegistration.swift`.

- [ ] **Step 1: `relativeTo` ergänzen** — die beiden Helper ersetzen:
```swift
    static func manrope(size: CGFloat, weight: Font.Weight = .regular,
                        relativeTo style: Font.TextStyle = .body) -> Font {
        Font.custom(Theme.font, size: size, relativeTo: style).weight(weight)
    }

    static func atoll(size: CGFloat, weight: Font.Weight = .regular,
                      relativeTo style: Font.TextStyle = .body) -> Font {
        manrope(size: size, weight: weight, relativeTo: style)
    }
```
(Neuer Param hat Default `.body` → keine bestehenden Call-Sites brechen; alle skalieren ab Body.)

- [ ] **Step 2: Große Titel hochstufen.** `grep -rn "\.atoll(size: 2[4-9]\|\.atoll(size: 30" --include="*.swift" AtollCard` — bei diesen großen Titeln (z. B. SignInView „AtollCard" size 30, Onboarding-/Detail-Überschriften size ≥ 24) den Aufruf um `, relativeTo: .title2` ergänzen. (Nur die ≥24er; kleinere bleiben `.body`.)

- [ ] **Step 3: Build + Tests grün**
Run: `xcodegen generate && xcodebuild test -scheme AtollCard -destination 'platform=iOS Simulator,name=iPhone 16e,OS=26.2' 2>&1 | grep -iE "Executed [0-9]+ tests|TEST (SUCCEEDED|FAILED)" | tail -2` ; macOS-Build grün.

- [ ] **Step 4: Commit**
```bash
git add AtollCard/DesignSystem/FontRegistration.swift AtollCard/Features
git commit -m "feat(ios): Dynamic Type scaling for Manrope fonts (relativeTo)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Reduce Transparency + Reduce Motion

**Files:** Modify `AtollCard/DesignSystem/GlassBackground.swift`, `AtollCard/Features/Shell/FloatingTabBar.swift`, `AtollCard/Features/Shell/MyCardScreen.swift`, `AtollCard/Features/CardEditor/OnboardingView.swift`.

- [ ] **Step 1: GlassBackground Reduce-Transparency** — `GlassBackground` ersetzen:
```swift
struct GlassBackground: ViewModifier {
    var cornerRadius: CGFloat = 20
    @Environment(\.accessibilityReduceTransparency) private var reduceTransparency

    func body(content: Content) -> some View {
        content
            .background(glassFill)
            .clipShape(RoundedRectangle(cornerRadius: cornerRadius, style: .continuous))
            .overlay(
                RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                    .strokeBorder(Color.white.opacity(0.5), lineWidth: 1)
            )
            .shadow(color: Color.black.opacity(0.10), radius: 20, x: 0, y: 8)
    }

    @ViewBuilder private var glassFill: some View {
        if reduceTransparency { Theme.surface } else { Color.clear.background(.ultraThinMaterial) }
    }
}
```

- [ ] **Step 2: FloatingTabBar** — `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency` + `@Environment(\.accessibilityReduceMotion) private var reduceMotion` ergänzen. Das `.background(.ultraThinMaterial)` (Zeile ~39) durch eine bedingte Fläche ersetzen: bei `reduceTransparency` → `.background(Theme.surface)`, sonst `.background(.ultraThinMaterial)`. Den `withAnimation(.snappy(duration: 0.2)) { selection = item.tab }` (Zeile ~20) ändern zu:
```swift
                    if reduceMotion { selection = item.tab }
                    else { withAnimation(.snappy(duration: 0.2)) { selection = item.tab } }
```

- [ ] **Step 3: MyCardScreen Material** — die `Capsule().fill(.ultraThinMaterial)` (Zeile ~188): `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency` zur View ergänzen und `.fill(reduceTransparency ? AnyShapeStyle(Theme.surface) : AnyShapeStyle(.ultraThinMaterial))` verwenden (oder via `@ViewBuilder`-Helper analog GlassBackground).

- [ ] **Step 4: OnboardingView Reduce-Motion** — `@Environment(\.accessibilityReduceMotion) private var reduceMotion` ergänzen; die beiden `withAnimation { step -= 1 }` / `withAnimation { step += 1 }` (Zeilen ~211/260) gaten:
```swift
            if reduceMotion { step -= 1 } else { withAnimation { step -= 1 } }
```
und analog für `step += 1`.

- [ ] **Step 5: Build beide Plattformen + Tests grün**
```
xcodegen generate
xcodebuild test  -scheme AtollCard -destination 'platform=iOS Simulator,name=iPhone 16e,OS=26.2'
xcodebuild build -scheme AtollCard -destination 'platform=macOS,arch=arm64'
```

- [ ] **Step 6: Commit**
```bash
git add AtollCard/DesignSystem/GlassBackground.swift AtollCard/Features
git commit -m "feat(ios): reduce-transparency glass fallback + reduce-motion gating

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Verifikationsgate (Screenshots)

**Files:** keine.

- [ ] **Step 1: Suiten + Builds grün** (iOS test, macOS build).
- [ ] **Step 2: Controller — Dark/Light + Dynamic-Type-Screenshots.** App auf Sim installieren/starten; `xcrun simctl ui "iPhone 16e" appearance dark` → Screenshot SignInView; `appearance light` → Screenshot; `xcrun simctl ui "iPhone 16e" content_size accessibility-extra-large` → Screenshot (Text größer, nichts abgeschnitten/überlappend). Zurücksetzen auf `medium`/`light`.
- [ ] **Step 3:** Visuelle Bestätigung dokumentieren; offene Funde notieren.

---

## Self-Review
- **Spec-Abdeckung:** dynamische Tokens + `Color(light:dark:)` + text2-Fix (T1), Dynamic Type relativeTo + große Titel (T2), Reduce-Transparency GlassBackground+FloatingTabBar+MyCardScreen + Reduce-Motion FloatingTabBar+Onboarding (T3), Screenshot-Gate (T4). ✓
- **Platzhalter:** keine; voller Code für Theme, Font, GlassBackground; gezielte Edits mit Zeilenhinweisen + Code für die Material/Animation-Stellen. ✓
- **Typkonsistenz:** `Color(light:dark:)` (T1) wie in Theme-Tokens genutzt; `Theme.surface` als Fallback in T1/T3 identisch; `Font.atoll(size:weight:relativeTo:)` (T2) abwärtskompatibel zu allen 54 bestehenden Calls. ✓
